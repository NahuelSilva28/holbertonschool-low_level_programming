#include "main.h"
/**
 *_strchr - locate a character
 *@s: the string
 *@c: character
 *Return: a pointer
 */
char *_strchr(char *s, char c)
{
	int i;

	for (i = 0; s[i] != '\0' ; i++)
	{
	}
	return (i);
}

