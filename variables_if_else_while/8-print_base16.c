#include <stdio.h>
/**
* main - base diez y seis
*
*
* Return: (0) Success
*/
int main(void)
{
	int n;
	char s;

	for (n = 0; n <= '9'; n++)
	putchar(n);
	{
	for (s = 'a'; s <= 'f'; s++)
		putchar(s);
	}
		putchar('\n');
return (0);
}
