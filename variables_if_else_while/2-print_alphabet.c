#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
* main - Entry point
* If and else
* Return: Always 0 (Success)
*/
int main()
for(int x = 'A'; x <= 'Z'; x++)
{
    putchar(tolower(x));
}
return (0)
}
