#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
* main - Entry point
* If and else
* Return: Always 0 (Success)
*/
int main(void)
{

char ns;
for (ns = 'a' ns <= 'z' ; ns++)
{
char(ns);
	}
	put char (ns);
return (0);
}
